# release-pr-check — Atlassian credentials
#
# 1. cp .env.example .env
# 2. 填入你的 email 和 API token，並儲存（.env 不要 commit）
# 3. 跑 release_pr_check.py 時會自動讀取
#
# API token 申請：https://id.atlassian.com/manage-profile/security/api-tokens
# (Atlassian → Profile → Security → Create API token)
#
# Token 看起來會像 "ATATT3xFf..." 開頭的長字串。
# 此 token 同時可用於 Confluence 和 Jira REST API。

ATLASSIAN_EMAIL=your.name@kkday.com
ATLASSIAN_API_TOKEN=ATATT3xFf...your_token_here...

# 可選：建立的 Confluence sub-page 要限制給哪些人看（comma 分隔的 emails）。
# 你自己的 email 一定會被加入，避免被自己鎖在外面。
# 不設定 = sub-page 對 space 內所有人可見。
# 也可以用 CLI flag --restrict-to 覆寫。
#
# RESTRICT_TO=lance.chien@kkday.com,jeremy.hsieh@kkday.com

# Optional: 若你還有 kkday-qa-tools repo 想用 get_secret fallback，
# 把該 repo 路徑加進 PYTHONPATH（不必設這個檔），script 會自動 detect。
